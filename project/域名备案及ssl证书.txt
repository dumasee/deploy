一、域名备案
服务器用域名访问，需要备案。
服务器只用ip访问，不需要备案。
如果服务器在国外，则不需要备案。
网站备案针对的是域名，域名解析对应的服务器更换不需再次备案。
域名备案后，二级子域名不需再次备案，做好域名解析后可直接使用。

移动云：
域名解析对应的服务器ip也需要进行备案登记。

二、ssl证书
ssl证书只与域名相关，更换主机或ip仍可利用原来的证书。
证书类型有：单域名证书、多域名证书、通配符证书。
免费证书一般是单域名或双域名证书。通配符证书要付费。
所有ssl证书到期只能新申请一个新证书，所谓续期也是申请新证书且老证书的剩余时间会加到新证书上不造成浪费。